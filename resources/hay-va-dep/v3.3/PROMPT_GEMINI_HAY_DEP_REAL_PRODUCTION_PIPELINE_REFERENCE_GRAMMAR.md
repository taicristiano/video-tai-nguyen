# GEMINI 3.8 FLASH HIGH
# HAY & ĐẸP. — ACTUAL PRODUCTION PIPELINE INTEGRATION
# REFERENCE SHOT GRAMMAR → REAL BATCH ENGINE
# OFFLINE ONLY
# ZERO CLOUDFLARE / SCHNELL CALLS
# ZERO IMAGE GENERATION
# ZERO FINAL VIDEO RENDER

## 0. BLOCKER FOUND BY SOURCE REVIEW

The latest template-level planner is NOT yet proven to be in the real production path.

Actual repository evidence:

```text
scripts/batch-engine.mjs
```

still executes:

```js
const storyResult = buildStoryPlan(...)
```

and still consumes:

```text
storyPlan.contentMode
storyPlan.castId
storyPlan.worldId
storyPlan.worldLock
storyPlan.beats
```

The existing production-compatible planner is:

```text
scripts/human-insight-story-planner.mjs
```

Meanwhile the new reference planner:

```text
src/templates/human-insight/cinematic-light/storyPlanner.ts
```

is currently consumed by acceptance/test tooling, not by the real batch engine.

There is also a concrete runtime issue in the current ZIP:

```text
scripts/batch-engine.mjs
```

calls `buildStoryPlan(...)` but does not import or define it.

This pass must fix the REAL production path, not create another acceptance harness.

---

# 1. PRIMARY GOAL

After this task:

```text
future video
→ scripts/batch-engine.mjs
→ production story planner
→ SAME reference-shot-grammar rules
→ story beats/spec
→ later image generation/render
```

The new grammar must affect every future HAY & ĐẸP. video generated by the normal batch workflow.

VIDEO005 and VIDEO013 are only offline acceptance fixtures.

---

# 2. DO NOT LOSE EXISTING STORY ENGINE FEATURES

The legacy production planner already owns important behavior:

```text
contentMode
castId
worldId
worldLock
continuityGroup
presentMembers
family interaction precedence
relationship member contracts
semantic visual beat splitting
canonical/question reuse rules
visualPriorities
assetStrategy
```

DO NOT replace it with a simpler planner that drops these features.

The correct architecture must COMBINE:

```text
existing semantic/cast/world Story Engine
+
new reference-derived shot grammar
```

not choose one and discard the other.

---

# 3. RUNTIME COMPATIBILITY MATTERS

`scripts/batch-engine.mjs` runs directly in Node.

Do NOT naïvely write:

```js
import { planHumanInsightVideo } from '../src/.../storyPlanner.ts'
```

because the current Node runtime/package setup does not directly load `.ts` modules.

Choose one real runtime architecture.

Preferred architecture:

```text
src/templates/human-insight/cinematic-light/
  referenceShotGrammarRuntime.mjs   <-- runtime source of truth
  storyPlannerRuntime.mjs           <-- reusable runtime transforms

scripts/human-insight-story-planner.mjs
  imports template runtime grammar
  preserves old content/cast/world logic
  applies reference grammar to final beats

TypeScript template files
  expose typed APIs / wrappers
  do NOT duplicate a second independent grammar implementation
```

An equivalent architecture is acceptable if:
- Node batch engine can execute it directly;
- one source of truth exists;
- no duplicated drifting grammar.

Do NOT add `tsx`/`ts-node` merely to bypass this unless absolutely necessary and justified.

---

# 4. FIX THE REAL BATCH ENGINE IMPORT

Audit:

```text
scripts/batch-engine.mjs
```

The current source calls:

```js
buildStoryPlan(...)
```

without a visible import/definition.

Fix it explicitly.

Example:

```js
import {
  buildStoryPlan,
} from './human-insight-story-planner.mjs';
```

or import the final canonical production entrypoint you create.

Add a test/static check so this cannot regress.

---

# 5. PRODUCTION STORY PLAN CONTRACT

The production `buildStoryPlan(...)` result must remain compatible with `batch-engine.mjs`.

Existing fields must survive:

```ts
{
  contentMode,
  castId,
  worldId,
  worldLock,
  continuityGroup,
  beats: [...]
}
```

Each final beat should additionally carry reference grammar fields as applicable:

```ts
{
  shotScale,
  silhouette,
  visualVerb,
  semanticIntent,
  composition,
  motionPreset,
  plannerStoryRole,
  assetStrategy,
  holdException?,
  grammarVersion: 'reference-shot-grammar-v1'
}
```

Do not force batch-engine to guess them again later.

---

# 6. CORRECT PIPELINE ORDER

Use this conceptual flow:

```text
canonical timeline
→ legacy semantic clause splitting
→ content mode / cast / world / member planning
→ role + visual intent/action
→ reference shot grammar:
     scale
     silhouette
     cadence
     anti-monotony
     composition
     hold validation
→ asset strategy
→ batch-engine spec generation
```

Reference grammar must enrich/normalize the existing semantic plan.

Do not rebuild a parallel unrelated set of beats.

---

# 7. REAL CADENCE SUPPORT

The runtime production grammar must enforce:

```text
18–22 visual changes/min
max 2 consecutive same scales
max 2 consecutive same silhouettes
normal image hold <= 4.0s
```

But story meaning stays first.

Do not mechanically alternate scales.

Do not remove cast/world semantics while normalizing cadence.

---

# 8. FIX TYPED HOLD EXCEPTION HANDLING

Current TypeScript `normalizeCadence()` still checks legacy:

```ts
!a.exceptionReason
!b.exceptionReason
```

while the new contract uses:

```ts
holdException
```

Remove this stale bypass logic.

Cadence merge/split must respect typed exceptions:

```ts
if (a.holdException || b.holdException) {
  do not merge blindly
}
```

or use:

```ts
isValidHoldException(...)
```

No free-form `exceptionReason` should control production behavior.

Remove/deprecate it from `PlannedShot` if no longer needed.

---

# 9. DO NOT OVERSTATE LOW-CADENCE NORMALIZATION

Current `normalizeCadence()` implementation visibly handles the “too fast → merge” direction.

If the production implementation does not really handle:

```text
too slow → semantic split
```

then either:

A. implement it correctly at template runtime level using semantic density/clauses;

or

B. state honestly that slow cadence is mainly solved upstream by semantic clause splitting, then validation rejects plans that remain <18 cpm.

Do not claim a capability the code does not contain.

---

# 10. BRAND PRODUCTION GATE MUST BE IN REAL BATCH FLOW

The current planner correctly detects:

```text
BRAND_AUDIO_MISMATCH
Nếp vs HAY & ĐẸP.
```

Now wire this to production.

Before ANY image generation or final render in normal batch flow:

```js
if (!storyResult.productionValidation.productionReady) {
  throw / stop with a clear production gate message
}
```

For DRAFT/planner-only mode:
- allow planning;
- surface warning;
- do not call image APIs.

For PRODUCTION:
- block before image generation.

Do NOT silently change spoken audio in this pass.

---

# 11. TEMPLATE BRAND CONTEXT

The batch workflow must pass:

```js
brandContext: {
  brandName: 'HAY & ĐẸP.',
  slogan: 'Điều hay để biết. Điều đẹp để giữ.'
}
```

from reusable channel/template config if such config exists.

Prefer configuration over hard-coding directly in `batch-engine.mjs`.

---

# 12. ACTUAL PRODUCTION DRY-RUN ENTRYPOINT

Add a true offline planner-only production dry run.

Example:

```text
node scripts/batch-engine.mjs --plan-only <videoKey>
```

or:

```text
node scripts/production-story-plan-smoke.mjs video005
```

It MUST execute the SAME planning function used by real batch production.

It must NOT:
- call TTS;
- call transcription;
- call Cloudflare;
- generate images;
- render video.

Output:

```text
story-plan.json
story-plan-validation.json
reference-grammar-metrics.json
production-readiness.json
```

This is the proof that the grammar is actually in production path.

---

# 13. TWO REAL PRODUCTION-PATH FIXTURES

Run the planner-only production path for:

```text
video005
video013
```

Verify:

```text
same production entrypoint
same grammarVersion
different narrative plan
cast/world metadata preserved
reference cadence/scale rules applied
```

For video005 expect brand production gate to remain BLOCKED until audio migration.

That is correct.

Do not fake READY.

---

# 14. TESTS

Add tests for the real production integration:

1. `batch-engine.mjs` has a valid production planner import;
2. actual production entrypoint returns a story plan;
3. existing `contentMode` survives;
4. existing `castId/worldId/worldLock` survive;
5. presentMembers / people contracts survive where applicable;
6. semantic visual beat splitting still works;
7. reference `shotScale` is present on production beats;
8. silhouette is present;
9. visualVerb/semanticIntent are present;
10. no 3 consecutive scale violations;
11. no 3 consecutive silhouette violations;
12. cadence is within 18–22 or correctly fails validation;
13. typed hold exceptions are respected;
14. legacy `exceptionReason` cannot bypass hold checks;
15. video005 uses production path + grammar v1;
16. video013 uses same production path + grammar v1;
17. PRODUCTION brand mismatch blocks before asset generation;
18. planner-only/draft mode makes zero external API calls;
19. old family/cast/question correctness tests still pass;
20. no video-specific fixture conditionals.

Run full repo tests.

---

# 15. SOURCE-LEVEL PROOF REQUIRED

Final report must show actual relevant source snippets:

```text
scripts/batch-engine.mjs
scripts/human-insight-story-planner.mjs
template runtime grammar module(s)
```

Show the exact import/call chain proving:

```text
batch-engine
→ production story planner
→ reference shot grammar
```

Do not use only a diagram.

---

# 16. ZIP / SECURITY

Do not rebuild a 100MB ZIP unless necessary.

No secrets.

No `.env`.

No generated media.

This task is code correctness only.

---

# 17. FINAL REPORT FORMAT

Return:

## A. Actual Production Call Chain
Exact source files and imports.

## B. Legacy Features Preserved
content mode / cast / world / presentMembers / semantic splitting.

## C. Reference Grammar in Production
Exact reusable runtime functions now called.

## D. Batch Engine Runtime Fix
Show `buildStoryPlan` import / canonical production entrypoint.

## E. Typed Hold Fix
Confirm no legacy exceptionReason bypass.

## F. Brand Gate
Video005 should show productionReady=false because audio still says Nếp.

## G. Production Dry Run
video005 + video013 metrics.

## H. Tests
Exact counts.

## I. External Calls
Must be 0.

## J. Verdict

Exactly one:

```text
HAY & ĐẸP. REAL PRODUCTION PIPELINE + REFERENCE GRAMMAR — READY FOR HUMAN REVIEW
```

or

```text
HAY & ĐẸP. REAL PRODUCTION PIPELINE + REFERENCE GRAMMAR — FAIL
```

Then STOP.

Do NOT generate images.
Do NOT render final video.
