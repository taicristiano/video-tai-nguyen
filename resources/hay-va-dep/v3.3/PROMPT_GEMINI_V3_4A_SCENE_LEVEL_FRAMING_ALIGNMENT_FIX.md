# GEMINI 3.8 FLASH HIGH
# HAY & ĐẸP. — V3.4A SCENE-LEVEL FRAMING ALIGNMENT FIX
# NO IMAGE GENERATION
# REUSE THE 6 SELECTED CLEAN ASSETS

## PROBLEM

Human review of the actual rendered MP4 found that some clean images are visibly shifted to one side inside the video.

This is NOT an image-generation failure.

Root cause:
- the six clean assets are scene-level square illustrations;
- the comparison pilot still inherits legacy per-scene asymmetric composition metadata such as:
  - `editorial-left`
  - `editorial-right`
  - old `focalPoint`
- those presets were authored for earlier asset geometry, so they can place the new square illustration inside a left/right editorial panel and create a large blank area.

Observed visually:
- scene 3 / Shot 03 is strongly shifted left with excessive blank space on the right;
- scene 5 / Shot 05 has the same issue;
- centered scenes are otherwise acceptable.

Do NOT regenerate any images.
Do NOT call Cloudflare.
Do NOT change production manifests.

## REQUIRED FIX

Patch only the isolated scene-level comparison pilot generated by:

```text
scripts/generate-v34-clean-asset-pilot.mjs
```

Inside `generatePilotRootCode()`:

### 1. Normalize asymmetric legacy compositions

For scene-level clean assets only:

```ts
const comparisonComposition =
  scene.composition === 'editorial-left' || scene.composition === 'editorial-right'
    ? 'portrait-focus'
    : (scene.composition ?? 'portrait-focus');
```

Pass:

```tsx
composition={comparisonComposition}
```

Do NOT change production scene composition data.

### 2. Reset old focal-point bias

The new clean assets already contain their own internal subject placement.

For this comparison pilot only, do NOT reuse old asset-specific focal points.

Use centered/default focal behavior:

```tsx
focalPoint={undefined}
```

or equivalent 50/50 center if the component requires an explicit object.

### 3. Preserve everything else

Keep unchanged:
- selected clean assets;
- scene timing;
- `shotScale`;
- V3.4A `motionProfile`;
- `motionPreset`;
- hard cuts;
- title/subtitle;
- SectionCard / InsightCard;
- audio/SFX;
- `visualBeats={undefined}`.

### 4. Do not force every scene to portrait-focus

Only normalize:

```text
editorial-left
editorial-right
```

Keep valid centered presets such as:
- `portrait-focus`
- `detail-insert`
- `paper`
- other non-asymmetric presets

unless the existing implementation requires a safe centered fallback.

## TEST

Add/update an offline test asserting generated PilotRoot code:

```text
editorial-left/right -> portrait-focus for comparison pilot
focalPoint is not inherited from scene.focalPoint
visualBeats remains disabled
shotScale and motionProfile remain preserved
```

No API calls.

## RERENDER

Reuse the already selected 6 assets.

Run only:

```text
--finalize
```

to regenerate:

```text
scratch/v34/clean-asset-pilot/video001-motion-clean-assets.mp4
```

No new image generation.

## REPORT

Return:

### A. Root Cause
### B. Code Change
### C. Test Result
### D. Rerender Path
### E. Verdict

Exactly:

```text
V3.4A SCENE-LEVEL FRAMING ALIGNMENT FIX — PASS
```

or FAIL.

Then STOP.
