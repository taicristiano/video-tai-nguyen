---
name: update-config
description: Update one key in the project .env file without printing secret values. Trigger on the /update-config command.
---

# /update-config — Update Environment Configuration

Read and follow `docs/SKILL-UPDATE-CONFIG.md`.
